datetime
========

Show different times: UTC, local time, etc

.. runblock:: pycon

   >>> # Build at
   >>> import datetime
   >>> datetime.datetime.utcnow()  # UTC
   >>> datetime.datetime.now()  # local time
