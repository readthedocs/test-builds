version: 2

build:
  os: ubuntu-26.04
  tools:
    python: "3"

mkdocs:
  configuration: docs/mkdocs.yml
  fail_on_warning: false

python:
  install:
    - method: uv
      command: pip
      requirements: requirements.txt
