version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3"
  apt_packages:
    # Required for mkdocs-with-pdf
    - weasyprint

mkdocs:
  configuration: docs/mkdocs.yml
  fail_on_warning: false

python:
  install:
    - requirements: requirements.txt
