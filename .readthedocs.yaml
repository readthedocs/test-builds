version: 2

build:
  os: ubuntu-22.04
  jobs:
    post_checkout:
      - env
    pre_build:
      - env
  tools:
    python: "3"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: requirements.txt
