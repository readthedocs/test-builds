version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3"
  commands:
    - pip install -r requirements.txt
    - jupyter-book create book/
    - jupyter-book build book/
    - mv --verbose book/_build/html/ $READTHEDOCS_OUTPUT/html/
    - find $READTHEDOCS_OUTPUT
