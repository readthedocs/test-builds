version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    - uname -a > docs/uname.txt
    - python3 -m pip install --quiet -r requirements.txt
    - python3 -m sphinx -T -b html -d _build/doctrees docs _readthedocs/html
