version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: latest
    nodejs: latest
    ruby: latest
    rust: latest
    golang: latest

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: requirements.txt
