version: 2

build:
  os: ubuntu-24.04
  tools:
    python: latest
  jobs:
    build:
      html:
        - zensical build
    post_build:
      - mv site/* $READTHEDOCS_OUTPUT_DIR/html/

python:
  install:
    - requirements: docs/requirements.txt
