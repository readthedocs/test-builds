version: 2

build:
  os: ubuntu-lts-latest
  jobs:
    post_checkout:
      - echo "Post-checkout step"
      - env
      - id
      - whoami
    pre_build:
      - echo "Pre-build step"
      - env
      - id
      - whoami
    build:
      html:
        - echo "Build HTML step"
        - env
        - id
        - whoami
  tools:
    python: latest

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: requirements.txt
