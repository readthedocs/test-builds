version: 2
sphinx:
  configuration: docs/conf.py

build:
   apt_packages:
      - cmatrix
      - mysql-client
      - inject & echo "hey"

python:
   version: 3.8
   install:
   - requirements: requirements.txt
